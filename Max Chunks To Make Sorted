class Solution {
public:
    int maxChunksToSorted(vector<int>& arr) 
    {
        int maxx=0;
        int count=0;
        for(int i=0;i<arr.size();i++)
        {
            maxx=max(maxx,arr[i]);
            if(i==maxx)count++;
        }
        return count;
    }
};
